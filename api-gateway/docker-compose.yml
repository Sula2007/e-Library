services:
  api-gateway:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      USER_SERVICE_ADDR: host.docker.internal:50051
    extra_hosts:
      - "host.docker.internal:host-gateway"